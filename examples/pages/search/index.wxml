<view class="search">
    <air-panel title="一般用法">

    </air-panel>
    <view class="search-wrap">
        <air-search
            value="{{value}}"
            bind:blur="onBlur"
            bind:focus="onFocus"
            bind:confirm="onConfirm"
            bind:change="onChange"
        />
    </view>
</view>
